import React from 'react';
import {
  View,
  Text,
  StyleSheet,
  TouchableOpacity,
  ImageBackground,
  ScrollView,
} from 'react-native';
import { Ionicons } from '@expo/vector-icons';

const WelcomeScreen = ({ navigation }) => {
  return (
    <ImageBackground
      source={{ uri: 'https://images.unsplash.com/photo-1414235077428-338989a2e8c0?ixlib=rb-1.2.1&auto=format&fit=crop&w=1350&q=80' }}
      style={styles.background}
    >
      <View style={styles.overlay}>
        <ScrollView contentContainerStyle={styles.container}>
          <View style={styles.logoContainer}>
            <Ionicons name="restaurant" size={80} color="#e67e22" />
            <Text style={styles.logoText}>Christofel</Text>
          </View>
          
          <Text style={styles.title}>Christofel Culinary Services</Text>
          <Text style={styles.subtitle}>
            Experience exquisite dining in the comfort of your own home. 
            Our private chef services bring restaurant-quality meals to your table.
          </Text>

          <TouchableOpacity
            style={styles.button}
            onPress={() => navigation.navigate('Menu')}
          >
            <Ionicons name="fast-food" size={24} color="white" />
            <Text style={styles.buttonText}>View Tonight's Menu</Text>
          </TouchableOpacity>

          <View style={styles.featuresContainer}>
            <View style={styles.feature}>
              <Ionicons name="star" size={32} color="#e67e22" />
              <Text style={styles.featureTitle}>Premium Quality</Text>
              <Text style={styles.featureText}>Finest ingredients sourced locally</Text>
            </View>

            <View style={styles.feature}>
              <Ionicons name="home" size={32} color="#e67e22" />
              <Text style={styles.featureTitle}>At Your Home</Text>
              <Text style={styles.featureText}>We come to you for convenience</Text>
            </View>

            <View style={styles.feature}>
              <Ionicons name="person" size={32} color="#e67e22" />
              <Text style={styles.featureTitle}>Expert Chef</Text>
              <Text style={styles.featureText}>Years of culinary experience</Text>
            </View>
          </View>
        </ScrollView>
      </View>
    </ImageBackground>
  );
};

const styles = StyleSheet.create({
  background: {
    flex: 1,
  },
  overlay: {
    flex: 1,
    backgroundColor: 'rgba(0,0,0,0.7)',
    padding: 20,
  },
  container: {
    flexGrow: 1,
    justifyContent: 'center',
    alignItems: 'center',
    paddingVertical: 40,
  },
  logoContainer: {
    alignItems: 'center',
    marginBottom: 30,
  },
  logoText: {
    fontSize: 36,
    fontWeight: 'bold',
    color: '#e67e22',
    marginTop: 10,
  },
  title: {
    fontSize: 28,
    fontWeight: 'bold',
    color: 'white',
    textAlign: 'center',
    marginBottom: 15,
  },
  subtitle: {
    fontSize: 16,
    color: 'white',
    textAlign: 'center',
    lineHeight: 24,
    marginBottom: 40,
    paddingHorizontal: 20,
  },
  button: {
    backgroundColor: '#e67e22',
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: 30,
    paddingVertical: 15,
    borderRadius: 10,
    marginBottom: 50,
  },
  buttonText: {
    color: 'white',
    fontSize: 18,
    fontWeight: 'bold',
    marginLeft: 10,
  },
  featuresContainer: {
    width: '100%',
  },
  feature: {
    backgroundColor: 'rgba(255,255,255,0.1)',
    padding: 20,
    borderRadius: 10,
    alignItems: 'center',
    marginBottom: 15,
    backdropFilter: 'blur(10px)',
  },
  featureTitle: {
    color: 'white',
    fontSize: 18,
    fontWeight: 'bold',
    marginTop: 10,
    marginBottom: 5,
  },
  featureText: {
    color: 'white',
    textAlign: 'center',
    fontSize: 14,
  },
});

export default WelcomeScreen;